# Knowledge Base

Централизованная база знаний, где контент создается и поддерживается ИИ-агентом по строгой, предсказуемой структуре.

## Ключевой принцип

**Одна статья = одна папка = один файл `README.md`.**

## Где что находится

- `inbox/` — единственная точка входа для новых материалов пользователя. Сюда загружаются файлы, которые позже обрабатываются агентом.
- `kb/ru/` — каноничная база знаний на русском языке. Содержит только готовые статьи.
- `kb/ru/INDEX.md` — центральный индекс всех статей.
- `_templates/` — шаблоны оформления статей.
- `prompts/` — рабочие промпты для агента.

## Правила добавления контента

1. Пользователь складывает материалы в `inbox/` без ручной сортировки.
2. Агент классифицирует материалы, создает темы и статьи.
3. Готовый контент публикуется только в `kb/ru/`.
4. Индексы обновляются автоматически.

См. шаблон статьи: `_templates/article.md`.

## Проверки

- Проверка ссылок в базе знаний: `python tests/check_kb_links.py`.

## VS Code и GitHub Copilot Chat

Для настройки GitHub Copilot Chat в VS Code используйте официальную инструкцию GitHub.
