# Входящие материалы (Inbox)

## Назначение

Папка `inbox/` — это **единственная точка входа** для новых материалов в базу знаний.

## Что сюда помещать

Загружайте в эту папку любые материалы, которые вы хотите добавить в базу знаний:

- 📝 Файлы в формате Markdown (`.md`)
- 📄 Документы (`.pdf`, `.docx`, `.txt`)
- 📊 Таблицы (`.xlsx`, `.csv`)
- 🗂️ Архивы (`.zip`, `.tar.gz`)
- 📋 Заметки и черновики
- 🔗 Ссылки на внешние ресурсы (в виде текстовых файлов)

## Важные правила

### ✅ Что делать:

1. **Просто загружайте файлы** — не нужно сортировать или организовывать их вручную
2. **Можете загружать несколько файлов одновременно** — агент обработает их все
3. **Используйте понятные имена файлов** — это поможет агенту правильно классифицировать контент

### ❌ Чего НЕ делать:

1. **Не создавайте подпапки** — вся структура будет создана автоматически в `kb/ru/`
2. **Не редактируйте файлы после загрузки** — агент обработает их в исходном виде
3. **Не ожидайте, что файлы останутся здесь** — после обработки они считаются временными

## Как это работает

### Процесс обработки:

```
1. Вы загружаете файлы → inbox/
2. Запускаете ИИ-агента
3. Агент:
   ├── Анализирует контент
   ├── Определяет темы
   ├── Создаёт статьи в kb/ru/
   ├── Обновляет индексы
   └── Помечает файлы как обработанные
4. Готовые статьи появляются в базе знаний
```

## Текущее состояние

> Эта папка пуста. Загрузите сюда файлы для начала работы с базой знаний.

## Что дальше?

После того, как агент обработает файлы:

1. Проверьте [центральный индекс](/kb/ru/INDEX.md) — там появятся новые статьи
2. Перейдите к соответствующей теме в `kb/ru/<topic>/`
3. Ознакомьтесь с созданными статьями
4. При необходимости предоставьте дополнительные материалы для уточнения

## Примеры использования

### Пример 1: Загрузка инструкции

```
inbox/
└── инструкция_по_настройке_сервера.pdf
```

→ Агент создаст статью в `kb/ru/infrastructure/server-setup/README.md`

### Пример 2: Загрузка нескольких связанных файлов

```
inbox/
├── введение_в_python.md
├── python_best_practices.docx
└── примеры_кода.txt
```

→ Агент может создать:
- `kb/ru/programming/python-intro/README.md`
- `kb/ru/programming/python-best-practices/README.md`
- Или объединить в одну статью, если контент связан

### Пример 3: Загрузка архива документации

```
inbox/
└── project_documentation.zip
```

→ Агент распакует и обработает каждый документ отдельно

---

**Готовы начать?** Загрузите ваши первые файлы в эту папку!
